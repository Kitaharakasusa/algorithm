/**
 * @param {string} s1
 * @param {string} s2
 * @return {boolean}
 */
var isScramble = function(s1, s2) {
    if(s1.length === s2.length) return false;
    if(s1 === s2) return true;

    let str1 = s1, str2 = s2;
    
};